// src/tokenizer.c
#include "tokenizer.h"

char    **tokenize_input(char *input) 
{
    // Split input string into tokens
    //a temp buffer that gets the text of the current token ->
    //-> make it a token and add to the linked list
    //look at the inputstr, and find the next token and do the same thing
}
